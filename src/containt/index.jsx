import React from 'react';
import PropTypes from 'prop-types';
import SlideBarFeature from '../features/slideBarFeature';
ContaintHeader.propTypes = {
    
};

function ContaintHeader(props) {
    return (
        <div>
            <SlideBarFeature/>
        </div>
    );
}

export default ContaintHeader;